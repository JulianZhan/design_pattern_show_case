package oberserpipeline;

public class PipelineRunner {
    public static void main(String[] args) {
        PipelineEntryPoint pipelineEntryPoint = new PipelineEntryPoint();
        pipelineEntryPoint.startPipeline();
    }
}
